To get the MessageAdmin running in your browser (and connected to a database):
 $ cd examples
 $ ./run-example

Then, browse to http://localhost:5280



Note: You'll need to have all the stuff for mdb going, independently of this (see Weaver).